// import * as api from '~/api'
// import cookie from '~/utils/cookie'

export default {
  // async getUserInfo ({ commit }) {
  //   let res
  //   try {
  //     res = await api.getUserInfo({ type: 'report', device_id: cookie.get('device_id') })
  //   } catch (err) {
  //     return
  //   }
  //   if (res.data.code === 0) {
  //     commit('GET_USER_INFO', res.data.data)
  //   }
  // }
}
